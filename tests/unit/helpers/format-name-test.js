import { formatName } from 'iibbsy/helpers/format-name';
import { module, test } from 'qunit';

module('Unit | Helper | format name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatName([42]);
  assert.ok(result);
});
