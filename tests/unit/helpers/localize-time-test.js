import { localizeTime } from 'iibbsy/helpers/localize-time';
import { module, test } from 'qunit';

module('Unit | Helper | localize time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = localizeTime([42]);
  assert.ok(result);
});
