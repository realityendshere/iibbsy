import { ordinalSuffix } from 'iibbsy/helpers/ordinal-suffix';
import { module, test } from 'qunit';

module('Unit | Helper | ordinal suffix');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ordinalSuffix([42]);
  assert.ok(result);
});
